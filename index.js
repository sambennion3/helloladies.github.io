const router = async () => {


    const routes = [
        {path: "/", view: () => console.log("Viewing homepage")}
    ];
    const switcher = document.querySelector('.button');
    switcher.addEventListener('click', function(){
//add the stuff here to send it to the google doc
    });

    
}